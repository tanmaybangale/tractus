/* eslint-disable */
// this is an auto generated file. This will be overwritten

export const onCreateFromToMessage = `subscription OnCreateFromToMessage(
  $id: ID
  $from: String
  $to: String
  $message: String
) {
  onCreateFromToMessage(id: $id, from: $from, to: $to, message: $message) {
    id
    from
    to
    message
  }
}
`;
export const onUpdateFromToMessage = `subscription OnUpdateFromToMessage(
  $id: ID
  $from: String
  $to: String
  $message: String
) {
  onUpdateFromToMessage(id: $id, from: $from, to: $to, message: $message) {
    id
    from
    to
    message
  }
}
`;
export const onDeleteFromToMessage = `subscription OnDeleteFromToMessage(
  $id: ID
  $from: String
  $to: String
  $message: String
) {
  onDeleteFromToMessage(id: $id, from: $from, to: $to, message: $message) {
    id
    from
    to
    message
  }
}
`;
